Notes for next work session:
    Learn how to use the library "Logging" - needed for all proffessional projects
    Impliment proper handling if a find or find_all returns a None
        EX: 
            tag = soup.find("h2")
            title = tag.get_text(strip=True) if tag else None
    Use try except on:
        Netowrk connection:
            creating session
            getting requests

    Create Validation Layer
        Layers should add to meta data fields of dict:
        schema["validation_status"] = {PASS, PARTIAL, FAIL}
        schema["validation_errors"] = {list of errors ie.       [
                                                                    "missing: fight_round",
                                                                    "invalid_int: sig_strikes_landed",
                                                                    "parse_error: method"
                                                                ]
                                                    }
        schema["parser_version"] = version - change when logic of scraper changes
        schema["scraped_at_utc"] = datetime.now(timezone.utc).isoformat()

